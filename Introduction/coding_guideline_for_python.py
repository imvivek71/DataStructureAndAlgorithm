"""
Before starting to any python
code we should always write introduction about that module
for better understanding and clarity about the module/file


# Naming convention for module writing:

It should be in lower case
For better reading and all we can include the "_" instead of space

"""
# Class name should use CamelCase convention


class PepRules:

    def __init__(self, first_number, second_number):
        self.first_number = first_number
        self.second_number = second_number

    def show(self):
        __a = 12
        print(self.second_number, self.first_number)
